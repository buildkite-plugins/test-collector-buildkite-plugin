steps:
  - label: ":shell: Shellcheck"
    plugins:
      shellcheck#v1.1.2:
        files: hooks/**

  - label: ":sparkles:"
    plugins:
      plugin-linter#v2.0.0:
        id: test-collector

  - label: ":docker: :hammer:"
    plugins:
      docker-compose#v3.9.0:
        run: tests
